<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="textViewLeft" parent="AppTheme">
        <item name="android:layout_width">@dimen/zed</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">@dimen/pad16</item>
        <item name="android:textSize">@dimen/ts</item>
        <item name="android:color">@color/colorPrimary</item>
        <item name="android:gravity">right</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textStyle">italic</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:background">@color/leftColor</item>
    </style>

    <style name="textViewRight" parent="AppTheme">
        <item name="android:layout_width">@dimen/zed</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">@dimen/pad16</item>
        <item name="android:textSize">@dimen/ts</item>
        <item name="android:color">@color/colorPrimary</item>
        <item name="android:gravity">left</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:background">@color/rightColor</item>
    </style>

    <style name="nestedLinear" parent="AppTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">horizontal</item>
        <!--<item name="android:layout_marginTop">@dimen/one</item>-->

    </style>

</resources>
